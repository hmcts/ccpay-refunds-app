spring:
  cloud:
    propertiesvolume:
      prefixed: false
      enabled: true
      paths: /mnt/secrets/ccpay
      aliases:
        ccpay-refunds-api-POSTGRES-PASS: POSTGRES_PASSWORD
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key

